#include<iostream>
#include<cmath>
 
int main(){
using std::cin;
using std:: cout;
using std::string;

double x=3;
double y=4;
double a,b,c,d,e,f,g,h;
    
a= std::max(x,y);// max of the two number 
b= std::min(x,y); // min of the two number 
c= std::pow(x,y);// this is x to the power of y 
d= std::sqrt(y);// square root of the number 
e= std::abs(-3); // postive value of any number or how far the  number is from zero
f= std::round(3.14); //rounds the number 
g= std::ceil(3.14);// rounds up the number 









cout<< a<< "\n";
cout<<b << "\n";
cout<<c << "\n";
cout<<d << "\n";
cout<<e << "\n";
cout<<f << "\n";
cout<<g << "\n";
return 0;
 }